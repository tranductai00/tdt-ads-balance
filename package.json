{
  "name": "t-balance-vercel-meta-only",
  "version": "7.0.1",
  "private": true,
  "description": "T Balance v7.0.1 Meta-only - fix undefined updateNotificationStatus during boot",
  "main": "server.js",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "check": "node --check server.js && node --check server/store.js && node --check server/meta-runtime.js && node --check api/metaBridge.js && node --check api/healthz.js && node --check public/push-sw.js",
    "token-key": "node scripts/generate-token-key.js",
    "vapid:generate": "node scripts/generate-vapid-keys.js",
    "test": "NODE_PATH=tests/stubs NODE_ENV=test node tests/test_meta_billing_parser.js && NODE_PATH=tests/stubs NODE_ENV=test node tests/test_manual_ad_edit.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3",
    "web-push": "^3.6.7"
  },
  "type": "commonjs"
}