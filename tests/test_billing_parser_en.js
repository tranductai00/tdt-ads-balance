const fs=require('fs'), vm=require('vm'); const src=fs.readFileSync(require('path').join(__dirname,'..','extension_meta_billing','content.js'),'utf8'); let handler;
const text=`Current balance\n$342.09\nYou'll pay when\nYour balance reaches\n$3,167.95\nAnd on this date\nSeptember 15, 2026\nYou'll pay with\nMastercard •••• 1234`;
const ctx={console,setTimeout,clearTimeout,setInterval(){},Date,URL,location:{href:'https://business.facebook.com/billing_hub/payment_activity?asset_id=123456789'},document:{body:{innerText:text},documentElement:{},addEventListener(){},visibilityState:'visible'},window:{addEventListener(){}},MutationObserver:class{constructor(cb){} observe(){}},chrome:{runtime:{onMessage:{addListener(fn){handler=fn}},sendMessage(){}}}};ctx.globalThis=ctx;vm.createContext(ctx);vm.runInContext(src,ctx);let out;handler({type:'BILLING_SCRAPE'},null,r=>out=r);console.log(JSON.stringify(out,null,2)); if(!out.ok||out.account.nextBillingDate!=='2026-09-15'||out.account.cardLast4!=='1234')process.exit(1);
